# website for openify.it

## Setup
* ```gem install jekyll```
* ```gem install rake```
* ```gem install jekyll-less```
* (in case) ```gem install therubyracer```

## Run
```jekyll serve```

## Deploy
```rake site:deploy```
